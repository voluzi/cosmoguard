apiVersion: v2
name: cosmoguard
description: Cosmos node firewall + cache + auth gateway
type: application
# The chart publish workflow refuses a chart/vX.Y.Z tag unless `version`
# equals that tag and `appVersion` equals the newest stable vX.Y.Z tag.
version: 2.0.0
appVersion: 4.0.3
kubeVersion: ">=1.24.0"
home: https://github.com/voluzi/cosmoguard
sources:
  - https://github.com/voluzi/cosmoguard
keywords:
  - cosmos
  - blockchain
  - proxy
  - firewall
  - tendermint
maintainers:
  - name: voluzi
