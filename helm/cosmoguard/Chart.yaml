apiVersion: v2
name: cosmoguard
description: Cosmos node firewall + cache + auth gateway
type: application
# Placeholders: a vX.Y.Z release tag stamps both version and appVersion with
# X.Y.Z when it packages the chart (make helm.package). An install from this
# directory defaults to the `latest` image unless image.tag is set.
version: 0.0.0-dev
appVersion: latest
kubeVersion: ">=1.24.0"
home: https://github.com/voluzi/cosmoguard
sources:
  - https://github.com/voluzi/cosmoguard
keywords:
  - cosmos
  - blockchain
  - proxy
  - firewall
  - tendermint
maintainers:
  - name: voluzi
